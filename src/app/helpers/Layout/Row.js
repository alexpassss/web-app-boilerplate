import React from 'react'
import { Box } from '@material-ui/core'
import { Row } from 'react-bootstrap'

const Template = props => {
  return <Box component={Row} {...props} />
}

export default Template
