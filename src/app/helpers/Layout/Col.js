import React from 'react'
import { Box } from '@material-ui/core'
import { Col } from 'react-bootstrap'

const Template = props => {
  return <Box component={Col} {...props} />
}

export default Template
