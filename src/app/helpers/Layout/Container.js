import React from 'react'
import { Box } from '@material-ui/core'
import { Container } from 'react-bootstrap'

const Template = props => {
  return <Box component={Container} {...props} />
}

export default Template
